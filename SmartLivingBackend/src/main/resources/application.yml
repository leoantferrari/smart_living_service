spring:
  datasource:
    url: jdbc:postgresql://ello.postgres.database.azure.com:5432/smartLiving?user=ello&password=gibbiX12345&sslmode=require
    username: ello
    password: gibbiX12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true